# Client

React + Vite app for showing LAN users, connecting two browsers, and moving files through a WebRTC DataChannel.

Feature modules live in `src/modules/`. Each module owns its UI, hooks, protocol helpers, and a local `README.md` so future AI agents can load only the module summary before editing.
